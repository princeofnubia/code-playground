const bubbleSort = (arr) => {
    for (i = 0; i < arr.length - 1; i++) {
        let swapped = false
        for (j = 0; j < arr.length - i + 1; j++) {
            if (arr[j] > arr[j + 1]) {

            }
        }
    }
}